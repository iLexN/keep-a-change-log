{
    "name": "ilexn/keep-a-change-log",
    "description": "Create change log follow keepachangelog.com",
    "authors": [
        {
            "name": "iLexN",
            "email": "ilex.job@gmail.com"
        }
    ],
    "license": "MIT",
    "autoload": {
        "psr-4": {
            "Ilex\\ChangeLog\\": "src"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Ilex\\ChangeLog\\Tests\\": "tests/"
        },
        "files": [
            "tests/helper.php"
        ]
    },
    "require-dev": {
        "friendsofphp/php-cs-fixer": "^2.10",
        "phpstan/phpstan": "^0.9.1",
        "phpstan/phpstan-phpunit": "^0.9.3",
        "phpunit/phpunit": "^6.5",
        "squizlabs/php_codesniffer": "3.1.1"
    },
    "config": {
        "sort-packages": true
    },
    "scripts": {
        "ci": [
            "@phpcs",
            "@test"
        ],
        "dev":[
            "@cs-fix",
            "@phpcs",
            "@bug-check",
            "@test-dev"
        ],
        "cs-fix": "php-cs-fixer fix",
        "phpcs": "phpcs",
        "bug-check" : "phpstan analyse -l max -c phpstan.neon src tests",
        "docs" : "@php ./sami.phar update .sami",
        "test-dev" : "phpunit --coverage-text",
        "test": "phpunit"
    },
    "scripts-descriptions": {
        "phpcs": "Checks that the application code conforms to coding standard",
        "test": "Launches the preconfigured PHPUnit"
    }
}
